// const HOME = '/';
const HOME = 'abc'; // 仮
const LOGIN = '/login';
const SIGNUP = '/signup';
const OPERATIONS = {
    CREATE_ITEMS: '/',
    CHECKS: '/checks',
}

export { HOME, LOGIN, SIGNUP, OPERATIONS };